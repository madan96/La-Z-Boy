language: python
cache: pip
python:
  - 2.6
  - 2.7
  # PyPy versions
  - "pypy"
# command to install dependencies
install:
  - pip install .
  - pip install -r requirements.txt
script:
  - python La-Z-Boy/__main__.py

